<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed ? "Task is completed" : "Task not completed yet
" %></p>

<%= link_to 'Back to list', tasks_path %>
<%= link_to 'Delete', task_path(@task), method: :delete, data: { confirm: 'Do you really want to?' } %>
<%= link_to 'Edit', edit_task_path(@task) %>
